# Summary

- `discretize.py` CLI tool to discretize csv data
- `switch_features.py` partially implemented tool that will switch feature specified (e.g class feature) to a specified row (to-do: args parsing)